import os

CODE_DIR = os.path.dirname(__file__)
CALCULATION_DIR = os.path.join(os.path.dirname(CODE_DIR), "calculation")
ARTICLE_CALCULATION_DIR = os.path.join(os.path.dirname(CODE_DIR), "article calculations")
